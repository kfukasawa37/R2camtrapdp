% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/references.R
\name{ctdp_schema_references}
\alias{ctdp_schema_references}
\title{List the external (URL) references declared by a Table Schema}
\usage{
ctdp_schema_references(x)
}
\arguments{
\item{x}{A \link{TableSchema}, or a parsed table-schema list
(\code{jsonlite::fromJSON(..., simplifyVector = FALSE)}).}
}
\value{
A tibble with columns \code{resource}, \code{field} (\code{NA} for schema-level),
\code{key} (the JSON key carrying the URL), \code{category} and \code{url}. Categories:
\code{"semantic-mapping"} (skos:*), \code{"description-reference"}, \code{"example"},
\code{"schema-ref"} (the table schema's own URL).
}
\description{
List the external (URL) references declared by a Table Schema
}
\examples{
sch <- list(name = "deployments",
  fields = list(
    list(name = "captureMethod", type = "string",
         "skos:exactMatch" = "http://rs.tdwg.org/dwc/terms/samplingProtocol")))
ctdp_schema_references(sch)
}
