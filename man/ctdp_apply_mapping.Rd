% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mapping.R
\name{ctdp_apply_mapping}
\alias{ctdp_apply_mapping}
\title{Apply a column mapping to a data frame}
\usage{
ctdp_apply_mapping(df, mapping, drop_unmapped = FALSE)
}
\arguments{
\item{df}{A data.frame / tibble of input data.}

\item{mapping}{The mapping from source column names to target field names.
Accepted forms:
\itemize{
\item a named character vector where \strong{names are source columns} and \strong{values
are target fields}, e.g. \code{c(lat = "latitude", lon = "longitude")};
\item a data.frame / tibble with columns \code{source} and \code{target}.
}}

\item{drop_unmapped}{If \code{TRUE}, keep only the mapped target columns; if
\code{FALSE} (default) also keep unmapped columns as-is.}
}
\value{
A tibble with renamed columns.
}
\description{
Renames the columns of \code{df} from the source (spreadsheet) names to Camtrap DP
field names. Columns of \code{df} that are not mentioned in \code{mapping} are kept
unchanged, so columns that already use Camtrap DP field names pass through.
}
\examples{
raw <- data.frame(station = c("A01", "A02"), lat = c(35.1, 36.2))
ctdp_apply_mapping(raw, c(station = "deploymentID", lat = "latitude"))
}
