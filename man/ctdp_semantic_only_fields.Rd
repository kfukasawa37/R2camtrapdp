% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/references.R
\name{ctdp_semantic_only_fields}
\alias{ctdp_semantic_only_fields}
\title{Fields whose meaning is defined only by reference (not machine-validated)}
\usage{
ctdp_semantic_only_fields(x)
}
\arguments{
\item{x}{A \link{TableSchema} or a parsed table-schema list.}
}
\value{
A tibble: \code{resource}, \code{field}, \code{type}, \code{reason}, \code{urls}.
}
\description{
Reports fields that carry a semantic mapping (\verb{skos:*}) or a reference URL in
their \code{description} but have \strong{no} enforceable \code{enum} or \code{pattern}
constraint. For these fields Frictionless (and this package) can check the
type/format but not the controlled vocabulary, so the values should be
checked against the referenced authority manually.
}
