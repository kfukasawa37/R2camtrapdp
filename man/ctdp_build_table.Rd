% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_table.R
\name{ctdp_build_table}
\alias{ctdp_build_table}
\title{Build and validate a table against a Table Schema}
\usage{
ctdp_build_table(
  schema,
  data,
  mapping = NULL,
  datetime_merges = NULL,
  tz = "Asia/Tokyo",
  source = NULL,
  coerce = TRUE,
  stop_on_error = FALSE
)
}
\arguments{
\item{schema}{A \link{TableSchema} object.}

\item{data}{A data.frame / tibble of input data.}

\item{mapping}{Optional column mapping; see \code{\link[=ctdp_apply_mapping]{ctdp_apply_mapping()}}.}

\item{datetime_merges}{Optional list of date/time merge specs. Each element
is a list with \code{date_col}, \code{time_col}, \code{target} (and optionally \code{format}),
applied with \code{\link[=ctdp_merge_datetime]{ctdp_merge_datetime()}} \strong{after} mapping.}

\item{tz}{Time zone used for temporal coercion / merging.}

\item{source}{Issue \code{source} label; defaults to \code{"<resource>.csv"}.}

\item{coerce}{If \code{TRUE}, coerce columns to schema types before validating.}

\item{stop_on_error}{If \code{TRUE}, raise an error (with a summary) when the
table has validation errors instead of returning them.}
}
\value{
A list with elements \code{data} (the coerced tibble) and \code{issues} (an
issue table from \code{\link[=ctdp_issues]{ctdp_issues()}}).
}
\description{
Build and validate a table against a Table Schema
}
