% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation_report.R
\name{ctdp_parse_frictionless}
\alias{ctdp_parse_frictionless}
\title{Parse a Frictionless validation report into an issue table}
\usage{
ctdp_parse_frictionless(report, resource_paths = NULL, descriptor = NULL)
}
\arguments{
\item{report}{A Frictionless report as a list or JSON string.}

\item{resource_paths}{Optional named character vector mapping resource names
to file paths (e.g. \code{c(deployments = "deployments.csv")}) used to give a
friendlier \code{source}. Falls back to the report's own \code{place}/\code{name}.}

\item{descriptor}{Optional parsed \code{datapackage.json} (list). When supplied,
the offending value of a \strong{metadata} (package) error is resolved from the
descriptor using the property path in the error note (e.g.
\verb{contributors[].email}) and placed in the issue's \code{value} column. For
table/cell errors the failing \code{cell} value is used directly.}
}
\value{
An issue table (includes a \code{value} column with the offending value(s)
when known).
}
\description{
Accepts either a parsed list (from \link[jsonlite:fromJSON]{jsonlite::fromJSON}) or a JSON string,
supporting both Frictionless v4 and v5 report layouts.
}
