% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema.R
\name{TableSchema}
\alias{TableSchema}
\title{R6 class representing a Frictionless Table Schema}
\description{
\code{TableSchema} loads a Frictionless Table Schema (such as the Camtrap DP
\code{deployments} / \code{media} / \code{observations} table schemas) from a URL, a local
file, or an in-memory list, and exposes everything needed to build and
validate a data table against it: field names and types, the primary key,
foreign keys, missing-value tokens, and per-field constraints
(\code{required}, \code{unique}, \code{enum}, \code{minimum}, \code{maximum}, \code{minLength},
\code{maxLength}, \code{pattern}, \code{type}, \code{format}).

Because the structure is read from the schema itself, an arbitrary Camtrap DP
version and any custom / extra columns are handled automatically: any field
present in the supplied schema participates in validation.
}
\section{Public fields}{
  \if{html}{\out{<div class="r6-fields">}}
  \describe{
    \item{\code{resource}}{Resource name, e.g. \code{"deployments"}.}

    \item{\code{version}}{Camtrap DP version used to resolve the URL.}

    \item{\code{url}}{Resolved schema URL (if loaded from a URL).}

    \item{\code{name}}{Schema \code{name}.}

    \item{\code{title}}{Schema \code{title}.}

    \item{\code{description}}{Schema \code{description}.}

    \item{\code{fields}}{Named list of field definitions keyed by field name.}

    \item{\code{field_order}}{Character vector of field names, in schema order.}

    \item{\code{primaryKey}}{Character vector naming the primary key field(s).}

    \item{\code{foreignKeys}}{List of foreign-key definitions.}

    \item{\code{missingValues}}{Character vector of missing-value tokens.}

    \item{\code{raw}}{The raw parsed schema (list).}
  }
  \if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
  \itemize{
    \item \href{#method-TableSchema-initialize}{\code{TableSchema$new()}}
    \item \href{#method-TableSchema-field_names}{\code{TableSchema$field_names()}}
    \item \href{#method-TableSchema-field}{\code{TableSchema$field()}}
    \item \href{#method-TableSchema-required_field_names}{\code{TableSchema$required_field_names()}}
    \item \href{#method-TableSchema-field_type}{\code{TableSchema$field_type()}}
    \item \href{#method-TableSchema-requirements}{\code{TableSchema$requirements()}}
    \item \href{#method-TableSchema-empty_table}{\code{TableSchema$empty_table()}}
    \item \href{#method-TableSchema-coerce}{\code{TableSchema$coerce()}}
    \item \href{#method-TableSchema-external_references}{\code{TableSchema$external_references()}}
    \item \href{#method-TableSchema-semantic_only_fields}{\code{TableSchema$semantic_only_fields()}}
    \item \href{#method-TableSchema-check_schema}{\code{TableSchema$check_schema()}}
    \item \href{#method-TableSchema-validate}{\code{TableSchema$validate()}}
    \item \href{#method-TableSchema-clone}{\code{TableSchema$clone()}}
  }
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-TableSchema-initialize"></a>}}
\if{latex}{\out{\hypertarget{method-TableSchema-initialize}{}}}
\subsection{\code{TableSchema$new()}}{
  Create a \code{TableSchema}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{TableSchema$new(
  resource = NULL,
  version = "1.0.1",
  url_template = NULL,
  local_path = NULL,
  json = NULL,
  cache_dir = file.path(tempdir(), "camtrapdp-schemas"),
  use_cache = TRUE
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{resource}}{Resource name (used to pick a default URL template and to
label issues), e.g. \code{"deployments"}.}
      \item{\code{version}}{Camtrap DP version string, e.g. \code{"1.0.1"}.}
      \item{\code{url_template}}{URL containing the \verb{<version>} placeholder. If \code{NULL}
and \code{resource} is one of the standard tables, a default template is used.}
      \item{\code{local_path}}{Path to a local schema JSON file. Takes precedence over
the URL.}
      \item{\code{json}}{A pre-parsed schema list. Takes precedence over \code{local_path}.}
      \item{\code{cache_dir}}{Directory used to cache downloaded schemas.}
      \item{\code{use_cache}}{If \code{TRUE}, reuse a cached copy when present and cache new
downloads.}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-TableSchema-field_names"></a>}}
\if{latex}{\out{\hypertarget{method-TableSchema-field_names}{}}}
\subsection{\code{TableSchema$field_names()}}{
  Field names in schema order.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{TableSchema$field_names()}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-TableSchema-field"></a>}}
\if{latex}{\out{\hypertarget{method-TableSchema-field}{}}}
\subsection{\code{TableSchema$field()}}{
  Get a single field definition by name.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{TableSchema$field(name)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{name}}{Field name.}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-TableSchema-required_field_names"></a>}}
\if{latex}{\out{\hypertarget{method-TableSchema-required_field_names}{}}}
\subsection{\code{TableSchema$required_field_names()}}{
  Names of fields whose \code{constraints$required} is \code{TRUE}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{TableSchema$required_field_names()}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-TableSchema-field_type"></a>}}
\if{latex}{\out{\hypertarget{method-TableSchema-field_type}{}}}
\subsection{\code{TableSchema$field_type()}}{
  Type of a field (\code{"string"}, \code{"number"}, ...).
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{TableSchema$field_type(name)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{name}}{Field name.}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-TableSchema-requirements"></a>}}
\if{latex}{\out{\hypertarget{method-TableSchema-requirements}{}}}
\subsection{\code{TableSchema$requirements()}}{
  A tidy summary of the schema's requirements: one row per
field with its \code{type}, \code{format}, and constraints (\code{required}, \code{unique},
\code{enum}, \code{minimum}, \code{maximum}, \code{pattern}). Works for any version / flavor.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{TableSchema$requirements()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A tibble.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-TableSchema-empty_table"></a>}}
\if{latex}{\out{\hypertarget{method-TableSchema-empty_table}{}}}
\subsection{\code{TableSchema$empty_table()}}{
  Create an empty (0-row) tibble shell with one correctly typed
column per schema field, in schema order.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{TableSchema$empty_table()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A tibble with 0 rows.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-TableSchema-coerce"></a>}}
\if{latex}{\out{\hypertarget{method-TableSchema-coerce}{}}}
\subsection{\code{TableSchema$coerce()}}{
  Coerce a data frame to the schema. The result contains
\strong{every} schema field, in schema order: present columns are cast to
the schema type (date/datetime/time formatted as canonical strings) and
fields absent from the input are added as typed \code{NA} columns (Camtrap DP
CSVs are expected to carry all schema columns). Columns not present in
the schema are kept after the schema columns (custom columns) and a
warning is emitted listing them.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{TableSchema$coerce(df, tz = "Japan", complete = TRUE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{df}}{A data.frame / tibble.}
      \item{\code{tz}}{Time zone used when formatting date/datetime values supplied as
\code{POSIXt} / \code{Date}.}
      \item{\code{complete}}{If \code{TRUE} (default), include all schema fields, filling
absent ones with typed \code{NA}. If \code{FALSE}, keep only the supplied columns.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A tibble.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-TableSchema-external_references"></a>}}
\if{latex}{\out{\hypertarget{method-TableSchema-external_references}{}}}
\subsection{\code{TableSchema$external_references()}}{
  List the external (URL) references this schema declares
(semantic \verb{skos:*} mappings, reference URLs in field descriptions, the
schema URL itself). See \code{\link[=ctdp_schema_references]{ctdp_schema_references()}}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{TableSchema$external_references()}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-TableSchema-semantic_only_fields"></a>}}
\if{latex}{\out{\hypertarget{method-TableSchema-semantic_only_fields}{}}}
\subsection{\code{TableSchema$semantic_only_fields()}}{
  List fields whose meaning is defined only by reference (a
semantic mapping or a description URL) and which therefore cannot be
fully validated against a controlled vocabulary. See
\code{\link[=ctdp_semantic_only_fields]{ctdp_semantic_only_fields()}}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{TableSchema$semantic_only_fields()}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-TableSchema-check_schema"></a>}}
\if{latex}{\out{\hypertarget{method-TableSchema-check_schema}{}}}
\subsection{\code{TableSchema$check_schema()}}{
  Check that this schema is a well-formed Frictionless Table
Schema (supported types, constraints valid for each type, keys reference
defined fields). See \code{\link[=ctdp_check_schema]{ctdp_check_schema()}}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{TableSchema$check_schema()}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-TableSchema-validate"></a>}}
\if{latex}{\out{\hypertarget{method-TableSchema-validate}{}}}
\subsection{\code{TableSchema$validate()}}{
  Validate a data frame against the schema constraints.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{TableSchema$validate(
  df,
  source = paste0(self$resource \%||\% self$name, ".csv"),
  raw = NULL
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{df}}{A data.frame / tibble (ideally already passed through
\verb{$coerce()}).}
      \item{\code{source}}{Label used as the issue \code{source} (e.g. \code{"deployments.csv"}).}
      \item{\code{raw}}{Optional pre-coercion data (the original input passed to
\verb{$coerce()}). When supplied, values that were present in \code{raw} but became
\code{NA} during coercion -- i.e. type-invalid entries such as a non-numeric
string in a \code{number} field -- are reported as \code{type} errors instead of
silently vanishing. \code{\link[=ctdp_build_table]{ctdp_build_table()}} passes this automatically.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    An issue table (see \code{\link[=ctdp_issues]{ctdp_issues()}}).
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-TableSchema-clone"></a>}}
\if{latex}{\out{\hypertarget{method-TableSchema-clone}{}}}
\subsection{\code{TableSchema$clone()}}{
  The objects of this class are cloneable with this method.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{TableSchema$clone(deep = FALSE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{deep}}{Whether to make a deep clone.}
    }
    \if{html}{\out{</div>}}
  }
}

}
