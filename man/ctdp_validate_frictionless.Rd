% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frictionless.R
\name{ctdp_validate_frictionless}
\alias{ctdp_validate_frictionless}
\title{Validate an existing Camtrap DP directory with Python Frictionless}
\usage{
ctdp_validate_frictionless(
  directory,
  python = "python",
  script = NULL,
  patch_profile = TRUE,
  summarize = TRUE
)
}
\arguments{
\item{directory}{Directory containing \code{datapackage.json} and its CSV files.}

\item{python}{Path to the Python interpreter (with \code{frictionless} installed).}

\item{script}{Path to \code{frictionless_validate.py}; resolved automatically when
\code{NULL} (option, then installed \verb{inst/python/}, then loose-source path).}

\item{patch_profile}{If \code{TRUE}, work around the malformed \verb{$ref} in the
Camtrap DP 1.0 profile by validating against a locally corrected copy. This
writes two \strong{new} helper files (\code{camtrap-dp-profile.patched.json},
\code{datapackage.validate.json}) into \code{directory} only when the profile is
actually malformed; it never modifies \code{datapackage.json} or the CSVs. Set
\code{FALSE} to keep the directory strictly read-only.}

\item{summarize}{Whether to print a summary.}
}
\value{
An issue table (see \code{\link[=ctdp_issues]{ctdp_issues()}}); engine \code{"frictionless"}.
}
\description{
Validates a data package that already exists on disk (e.g. created by another
tool or in a previous run) \strong{without writing or overwriting} the
\code{datapackage.json} or the CSV files. This is the validate-only counterpart of
the \code{R6_CamtrapDP$validate_frictionless()} method, which (by default) rewrites
the package from the R object before validating.
}
\examples{
\dontrun{
# Validate an existing Camtrap DP on disk (needs Python with 'frictionless'):
issues <- ctdp_validate_frictionless("path/to/camtrapdp", python = "python")
ctdp_is_valid(issues)
}
}
